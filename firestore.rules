rules_version = '2';

// GauSeva Connect stores application data in Cloudflare D1 via authenticated APIs.
// Firestore is not used for app data. Deny all direct client access by default.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
